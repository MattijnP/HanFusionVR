using UnityEngine;
using UnityEngine.Video;

public class BeamerIntro : MonoBehaviour
{
    [SerializeField]
    private VideoPlayer VideoSource;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
